Configuration:
  status: warn

  Appenders:
    File:
      name: logfile
      fileName: logs/logs.log
      PatternLayout:
        Pattern: "%d{HH:mm:ss} %-5level | %-20class{1} | %msg%n"

  Loggers:
    Root:
      level: debug
      AppenderRef:
        ref: logfile